# Sample corpora are COMMITTED (image + text + splits) so a fresh clone runs out of the box.
# Only these local artifacts stay ignored:
datasets/splits/spot_check/
datasets/splits/manifest.pre_repair_backup.csv
models/artifacts/
runs/
benchmarks/results/
# CLI outputs written next to the analyzed file (rescan skips them anyway)
*.understanding.json
*.understanding.md
__pycache__/
*.pyc
.venv/
venv/
.env
.vs/ProjectSettings.json
*.vsidx
/.vs
AidenOCR/
.pytest_cache/
.ruff_cache/
.rcn_theme
